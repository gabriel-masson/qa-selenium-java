# Ambiente: DEV
# Executado a cada push em branch de feature — feedback rápido, suíte reduzida.

base.ui.url=https://automationexercise.com
base.api.url=https://automationexercise.com/api

browser=chrome
headless=false

# Timeouts mais permissivos localmente (rede do dev pode variar)
timeout.implicit.seconds=5
timeout.explicit.seconds=10
