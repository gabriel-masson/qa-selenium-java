# Ambiente: PRODUÇÃO
# Executado pós-merge/deploy — apenas smoke tests dos fluxos críticos.

base.ui.url=https://automationexercise.com
base.api.url=https://automationexercise.com/api

browser=chrome
headless=true

# Timeouts mais curtos: smoke test deve ser rápido; qualquer lentidão é um sinal
timeout.implicit.seconds=5
timeout.explicit.seconds=8
