# Ambiente: STAGING
# Executado em Pull Requests — suíte completa (API + Integração + E2E).

base.ui.url=https://automationexercise.com
base.api.url=https://automationexercise.com/api

browser=chrome
headless=true

# Timeouts mais rígidos: já validamos estabilidade antes de chegar aqui
timeout.implicit.seconds=5
timeout.explicit.seconds=10
